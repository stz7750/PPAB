<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="store.stz7750.stz.mapper.userMapper">
    <select id="selectUserInfo" resultType="store.stz7750.stz.vo.userVO">
        SELECT
            *
        FROM
            userinfo
    </select>

    <insert id="insertUser" parameterType="store.stz7750.stz.vo.userVO">
        INSERT INTO
            userinfo
            (
                id,
                name,
                ip,
                os,
                browser,
                email
            )
            VALUES
            (
                #{id},
                #{name},
                #{ip},
                #{os},
                #{browser},
                #{email}
            )
    </insert>
</mapper>